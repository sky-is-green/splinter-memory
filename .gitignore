__pycache__/
*.py[cod]
*$py.class
*.so
*.egg-info/
dist/
build/
.eggs/
*.egg
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
.hypothesis/

# Python environment
.env
.venv/
venv/
env/
.Python

# Model weights (downloaded, not authored)
models/
*.pt
*.pth
*.safetensors
*.bin
*.gguf
*.onnx

# HuggingFace cache
.cache/
.huggingface/
.cache/huggingface/

# Embedding / vector caches
*.npy
*.npz
cache/
splinter/sieve/cache/
*.h5

# Trained / serialized artifacts
*.pkl
*.joblib
*.ckpt
*.safetensors.bak

# Runtime state (context store, decay matrix, splinter checkpoints)
state/
checkpoints/
splinter/retention/state/
*.jsonl

# Databases (ground truth, vault)
*.sqlite3
*.sqlite
*.db
*.db-journal

# Logs (rotated daily; never commit)
logs/*.ndjson
logs/*.json
*.log

# Ignore python virtual environments where torch/scipy live
.venv/
venv/
env/
site-packages/

# Ignore heavy ML binaries, model weights, and data streams
*.dll
*.lib
*.safetensors
*.ndjson

# Benchmark & report outputs (generated)
reports/
*.html

# Rendered paper figures (committed — embedded in the white paper)
!figures/*.png
!figures/*.svg

# Generated test data (large synthetic conversations; regenerate via
# python -m tests.fixtures.synthetic_conversations.generate [--prose|--horizon|--p9])
data/
sample_data/

# Generated data-generation runs (reports, logs, ground-truth DBs)
runs/
runs_mock/
# Harness sidecar conversation state
harness_state/

# Studio runtime artifacts (demo transcripts, session store, tool home)
transcripts/
.sessions/
.dsh-home/
tools/llama.cpp/

# Local runtime config (secrets / machine-specific)
*.local.json
config.local.*

# AI coordination / handoff documents (local working documents, not repo content)
HARNESS-SPEC.md
SPLINTER-HANDOFF.md
HANDOFF-*.md

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp
*~

# Local agent-tool config (machine-specific)
.opencode/
opencode.json
*.whl

# hive-ops coordination state (local-only; AGENTS.md and scripts/** are tracked)
QUEEN.md
BEE-GUARD.md
BEE-BETA.md
HIVE-OPS.md
HIVE-RESEARCH.md
PROPOSALS.md
SPLINTER-PLAN.md
.claims/
.bee-status/
hive-status.log
RESEARCH/
RESEARCH-QUEUE.md
*.local.md
